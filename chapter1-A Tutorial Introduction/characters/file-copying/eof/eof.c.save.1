#include <stdio.h>
/* copy input to output; 2nd version */
int main()
{
int c;
while ((c = getchar()) == EOF){
putchar(c);
//printf("here: %d", c);
}
#include <stdio.h>
1* copy input to output; 2nd version *1
maine )
{
int c;
while «c = getchar(»
putchar(c);
1= EOF)
}
}
