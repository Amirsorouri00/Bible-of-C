#include <ctype.h>
#include<stdio.h>
/* Defines a family of functions that provide tests and conversions 
that are independent of character set. for example, the function 
tolower(c) returns the lower case value of c if c us upper case. 

similarly isdigit(c) can be used to determine if the character is 
a digit or whatever else. */


